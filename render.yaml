services:
  - type: web
    runtime: static
    name: buildify-frontend

    rootDir: react_frontend

    buildCommand: npm ci && npm run build

    staticPublishPath: build

    routes:
      - type: rewrite
        source: /*
        destination: /index.html